# Definitions file to be used globally
import os

ROOT_DIR = os.path.dirname(os.path.abspath(__file__)) # This is your Project Root
CONFIG_DIR_NAME = "config/"
CONFIG_PATH = ROOT_DIR + "/" + CONFIG_DIR_NAME
